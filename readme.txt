The new user script must be run from the exchange management shell. In order to import the exchange cmdlets, c:\exchange.ps1 should exist. If it doesn't, copy from the template and edit for the client.

Open the exchange management shell, type .\d:\git\scripts\auto-user-provision.ps1, follow prompts